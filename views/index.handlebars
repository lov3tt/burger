<div class="container-fluid bg">

<div class="jumbotron text-center">
  <h1 class="display-4 text-center">EAT-DA-BURGER!</h1>
  <h3 class="lead">When you want to eat a burger, but care about your weight.</h3>
  <hr class="my-4">
  <h2>Now you can eat all the burgers you want virtutally free and it won't hurt your weight gain!</h2>
<img id="img">
</div>



<div class="jumbotron">
  <div class="row text-center">
    <div class="col-6">
      <h2>Burgers Available!</h2>
    </div>
    <div class="col-6">
      <h2>Burgers Eaten!</h2>
    </div>
  </div>
  <hr class="my-4"> 
   <div class="row">
     <div class="col-2"></div>
    <div class="col-4">
      <ul class="open" >
        {{#each burgers}}
        {{#unless devoured}}
        {{> burgers/burger-block devour=true}}
        {{/unless}}
        {{/each}}
      </ul>
      </div>
     
    <div class="col-4 text-left">
      <ul class="close" >
        {{#each burgers}}
        {{#if devoured}}
        {{> burgers/burger-block devour=false}}
        {{/if}}
        {{/each}}
      </ul>
    </div>
    <div class="col-2"></div>
  </div>

  <hr class="my-4">

    <h2>Add a Burger!</h2>
  <form class="create-form">

    <div class="form-group">
      <label for="ca">Burger Name:</label>
      <input type="text" id="ca" burger_name="burger_name">
    </div>
 <hr class="my-4">
    <button class="btn btn-primary btn-lg" value="1" burger_name="devoured" type="submit">Add Your Burger!</button>
  </form>
</div>

</div>
